import { Button } from "@mui/material";
function GreenButton() {
  return (
    <Button
      style={{
        margin: 15,
        backgroundColor: "green",
        borderRadius: 10,
        padding: 16,
      }}
      variant="contained"
    >
      Tao luong moi
    </Button>
  );
}
export default GreenButton;
